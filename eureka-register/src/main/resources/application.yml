spring:
  application:
    name: register
server:
  port: 8180
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8180/eureka/


